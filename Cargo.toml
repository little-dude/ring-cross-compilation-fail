[package]
name = "cross-compile-reqwest"
version = "0.0.1"
authors = ["little-dude <corentinhenry@gmail.com>"]
edition = "2018"
description = "reproduce compilation error"

[dependencies]
ring = "0.16.15"

[lib]
name = "cross_compile_reqwest_lib"
crate-type = ["staticlib", "cdylib"]
